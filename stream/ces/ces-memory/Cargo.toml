[package]
name = "ces-memory"
version = "0.1.0"
edition = "2021"
description = "Three-tier memory: Engram + Hebbian LoRA + episodic"

[dependencies]
anyhow = { workspace = true }
candle-core = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
criterion = { workspace = true }

[lib]
name = "ces_memory"
